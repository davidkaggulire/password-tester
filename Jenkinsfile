Jenkinsfile (Declarative Pipeline)

pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'python passwordtester.py'
            }
        }
        stage('build') {
            steps {
                sh 'python testing.py'
            }
        }
    }
}

